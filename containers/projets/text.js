import { createComponent } from 'react-fela'

const text = () => ({
  maxWidth: '650px',
  margin: 0,
  padding: 0,
  color: '#fff',
  fontSize: 1.1,
  marginTop: 10,
})

export default createComponent(text, 'p')
