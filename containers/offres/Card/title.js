import { createComponent } from 'react-fela'

const cardTitle = () => ({
  margin: 0,
  padding: 0,
  marginBottom: 15,
  fontSize: 1.5,
  fontWeight: 400,
  textAlign: 'center',
  color: '#fff',
})

export default createComponent(cardTitle, 'h3')
